# @civicfix/contracts

Shared TypeScript types for the FastAPI `/v1` API contract, mirrored across `apps/mobile` and
`apps/web`. See status/lifecycle vocabulary in [`../../spec/ARCHITECTURE.md`](../../spec/ARCHITECTURE.md).
